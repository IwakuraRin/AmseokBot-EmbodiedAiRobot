# Core boundary

`core` owns application-wide infrastructure such as authentication, HTTP interceptors, startup configuration, and global error handling. It must not depend on built-in features or plugin implementations.
